# Weather App Frontend

## Setup
1. `cd frontend`
2. `npm install`
3. `npm run dev`

App runs on http://localhost:5173 and proxies `/api` to the backend at http://localhost:5000.
Make sure the backend is running first.
